#include <global.h>

void top_level()
{
    // fork off safety
/*
    zrovnavacia_jazda();
    
    while(1)
    {
        // fork off dorovnavanie, door a, door b
        // | | | |
        stopped();
        // kill threads

        if(wanna_go_up)
        {
            // fork off
            go_up();
        }
        else
        {
            // fork off
            go_down();
        }
        // kill threads
    }
*/
}